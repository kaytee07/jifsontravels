"use server"

export const signIn = () => {
    try {
        
    } catch (error) {
        console.error('Error', error)
    }
}

export const signUp = () => {
    try {
        
    } catch (error) {
        console.error('Error', error)
    }
}